# Environment Variables Configuration

## Required Variables

### Google Sheets Configuration
VITE_GOOGLE_SHEETS_ID=your_sheet_id_here
VITE_PUBLIC_SHEET_URL=your_public_sheet_url_here

### Application
VITE_APP_NAME=ImmoDash
VITE_APP_VERSION=2.5.0

## Setup Instructions

1. Copy this file to `.env`
2. Replace the placeholder values with your actual configuration
3. Never commit the `.env` file to version control

## Notes
- The Google Sheet must be published to the web for CSV export to work
- No API key is needed when using public sheet CSV export
