const fs = require('fs'); const wf = JSON.parse(fs.readFileSync('Bogbes multi service.json')); const node = wf.nodes.find(n => n.name === 'Sauvegarder Visite'); node.parameters.jsonBody = '={{ JSON.stringify({ numero:.prospect_numero, nom_prenom:.prospect_nom, ref_bien:.ref_bien, local_interesse:.bien_description, contact_proprietaire:.contact_proprietaire, date_rv:\\'A planifier\\', visite_prog:\\'Oui\\', created_at:new Date().toISOString() }) }}'; fs.writeFileSync('Bogbes multi service.json', JSON.stringify(wf, null, 2)); console.log('Fixed visit node');
